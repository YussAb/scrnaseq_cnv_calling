/*
 * Base process resource definitions.
 */

process {
    withLabel: process_medium {
        cpus   = 4
        memory = 16.GB
        time   = 6.h
    }

    withLabel: process_high {
        cpus   = 8
        memory = 32.GB
        time   = 6.h
    }
}
