/*
 * Lightweight local test overrides.
 */

process {
    withName: INFERCNV {
        cpus   = 1
        memory = 1.GB
        time   = 1.h
    }

    withName: COPYKAT {
        cpus   = 1
        memory = 1.GB
        time   = 1.h
    }
}
